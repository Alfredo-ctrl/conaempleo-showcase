# Public showcase placeholders only.
# Never commit real database, SMTP, API, or admin credentials.

APP_ENV=local
APP_URL=http://localhost:5188

DB_HOST=example-host
DB_NAME=example-database
DB_USER=example-user
DB_PASSWORD=replace-me

SMTP_HOST=example-smtp-host
SMTP_USER=example-smtp-user
SMTP_PASSWORD=replace-me

OPENAI_API_KEY=replace-me
